{{#each post in model.content}}
    <br>
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <i>(Posted on {{post.createdDate}} by {{post.author}})</i>
    <br>
    <br>
    <br>
    <hr>
{{/each}}
